{
  "name": "keyprism-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:i18n": "node scripts/check-i18n.mjs",
    "test:mixer": "node scripts/test-mixer.mjs",
    "test": "npm run check:i18n && npm run test:mixer"
  },
  "dependencies": {
    "plotly.js-dist-min": "^2.35.2"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
