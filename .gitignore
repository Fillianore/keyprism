# Python
__pycache__/
*.pyc
.venv/

# Node / frontend build
node_modules/
dist/

# Backend output artifacts (generated automatically when the backend runs)
frontend/public/data.json
frontend/public/audio.*

# Local tool state (AI coding agent sessions/worktrees, unrelated to the project)
.kilo/

# Note: uv.lock and frontend/package-lock.json are intentionally committed
# (dependency locking, CI reproducibility); .github/workflows is also
# intentionally committed (CI itself). Do not add them to this file.
